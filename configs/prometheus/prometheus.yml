global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Phase 5: FastAPI serving metrics
  # - job_name: "fastapi"
  #   static_configs:
  #     - targets: ["fastapi:8000"]

  # Phase 6: Evidently monitoring metrics
  # - job_name: "evidently"
  #   static_configs:
  #     - targets: ["evidently:8080"]
